import LoginButton from "./LoginButton";
import PageTitle from "./PageTitle";
const NavBar = () => {
  return (
    <nav>
      <PageTitle />
      <LoginButton />
    </nav>
  );
};

export default NavBar;
